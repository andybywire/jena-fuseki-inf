[main]
localhost = org.apache.jena.fuseki.authz.LocalhostFilter   # keep helper

[users]
admin = pwd

[urls]
/$/server   = anon
/$/ping     = anon
/$/stats/** = anon       # ← allow the UI's polling call
/$/**       = localhost  # everything else under /$/ still locked down
/**         = anon